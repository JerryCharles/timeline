# Use clean-install for consistent dependencies
use-clean-install=true

# Optimize installation
prefer-offline=true
cache-min=3600

# Allow dev dependencies for build process
production=false

# Reduce package size
fund=false
update-notifier=false 